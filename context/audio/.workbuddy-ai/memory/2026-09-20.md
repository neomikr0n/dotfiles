# 2026-09-20 — Registro de trabajo

## Seguimiento RME ADI-2 EX (automatización 078e7794)

Comprobación programada del precio oficial del ADI-2 DAC EX. **Sin novedad.**

- `rme-audio.de/converter/adi-2-dac-ex.html` → **sigue 404**.
- `rme-audio.de/converter/adi-2-4-pro-ex.html` → **sigue 404**.
- `rme-audio.de/products.html` y `rme-audio.de/news.html` → sólo aparece **ADI-2 Pro EX**;
  ningún DAC EX ni /4 Pro EX.
- `rme-shop.com/acatalog/RME_converters.html` → ADI-2 Pro EX €1.992,87; ADI-2 DAC FS
  €816,75 **agotado**; ADI-2/4 Pro SE €2.299 **agotado**. Nada de modelos EX nuevos.
- Comunicado 3-jun-2026 (musicnetwork.ch) sigue siendo la única referencia:
  DAC EX y /4 Pro EX = «late Q3 2026; pricing to be announced».
- México: `solidelectronics.mx/catalogsearch/result/?q=ADI-2` no lista ningún «EX».
  ADI-2 DAC FS $27,600 MXN (sin existencias), ADI-2/4 Pro SE $51,100 MXN (sin existencias).
- Novedad menor detectada: **Thomann ya retiró el ADI-2 DAC FS de catálogo**
  («ya no se encuentra en nuestro catálogo») — señal de transición al sucesor.
- Preventa de revendedor (no oficial): Reverb / Tidepool Audio (Portland OR) lista el
  **ADI-2/4 Pro EX a $2,499 USD** (mostrado como MEX$44,956.20), envío estimado 30-sep-2026.

## Análisis: ADI-2 DAC FS frente al ADI-2/4 Pro EX (11:38)

Consulta del usuario: análisis profundo de su RME frente al ADI-2/4 Pro EX, clasificar las
mejoras en marginales o significativas, y determinar si es pertinente considerarlo.

**Hallazgo principal: no son la misma categoría de equipo.** El DAC FS es un convertidor D/A
de escucha; el 2/4 Pro EX es una interfaz de masterización (2 canales A/D + 4 canales D/A).

**Dato clave: la conversión D/A está en la misma clase.**
- DAC FS: ESS ES9028Q2M, **123 dBA** y **THD+N −116 dB** según la web oficial de RME;
  **120 dBA** según el manual local (discrepancia registrada; el manual lo dice dos veces).
- 2/4 Pro SE: «hasta 123 dBA».
- 2/4 Pro EX: ESS9039Q2M con «mejora notable» pero **sin ninguna cifra publicada**.
→ Lo sonoro cambia de forma marginal.

**Amplificación de audífonos (verificado en el manual local del DAC):**
- DAC FS: **1.5 W @ 32 Ω**, +22 dBu Hi Power / +7 dBu Low Power, 0.1 Ω, THD < −110 dB a
  32 Ω, SNR 120 dBA, 0 Hz a 80 kHz. Salida IEM fija a **−3 dBu**, piso de ruido **−121 dBu(A)**.
- 2/4 Pro SE: **2.1 W** no balanceada / **3.4 W** balanceada @ 32 Ω. True Balanced:
  +7 dBu IEM, +13 dBu Low Power, **+25 dBu** High Power.
- Diferencia: +1.5 dB sin balancear, +3.6 dB balanceada. **Irrelevante**: los HE1000se
  (35 Ω, 96 dB) necesitan ~250 mW para 120 dB de SPL y el DAC ya entrega 1,500 mW.
  Además el usuario escucha por el Aune S17 Pro EVO, no por el RME.

**Dato arquitectónico verificado en el manual:** en el DAC FS «the rear RCA and XLR outputs
and the front outputs Phones and IEM are fed from the same DAC, hence carry the same
signal». El 2/4 Pro tiene caminos independientes (los audífonos balanceados usan los canales
DAC 3/4 y la salida trasera puede emitir otra señal por 1/2).

**Corrección evitada:** comprobé que el DAC FS **sí tiene** M/S Processing, Phase, Mono y
Width (manual §8.7, línea 629). No es un diferenciador frente al Pro. Casi lo di por supuesto
mal.

**Mejoras del EX sobre el SE (comunicado oficial RME del 3-jun-2026, texto literal):**
- Convertidor A/D **ESS9823Pro**; convertidor D/A **ESS9039Q2M**.
- **RIAA en modo línea (0 dB)**, que permite preamplificadores externos y cápsulas MC.
- **Tornillo de tierra** separado para tocadiscos.
- Niveles de referencia extendidos hasta **−35 dBu** → micrófonos dinámicos directos.
- **Canales ADAT 3/4** seleccionables como fuente de la salida de audífonos.
- **SteadyClock EX**: ±1 ppm de fábrica, ±50 ppm ajustable en pasos de 0.1 ppm.
- USB-C bloqueable, protección de fuente sin fusible, protección DC por canal.

**Ojo:** el aumento de potencia de audífonos que menciona el comunicado corresponde al
**ADI-2 Pro EX**, no al 2/4 Pro EX. No darlo por hecho para el 2/4.

**Precios (20-sep-2026):** 2/4 Pro EX sin precio oficial (sólo preventa de revendedor
$2,499 USD); 2/4 Pro SE $51,100 MXN / €2,299 (sin existencias); DAC FS $27,600 MXN /
€816.75 (sin existencias). Dato: el DAC FS cuesta €816.75 en Europa (~$15,300 MXN) frente a
los $27,600 MXN de la tienda mexicana — casi el doble.

**Veredicto entregado:** no es pertinente como sustituto del DAC FS — sería pagar ~4 veces
más por un bloque de E/S de estudio mientras la conversión D/A se queda en la misma clase.
Pertinente sólo si (1) hay tocadiscos, (2) se graba o se mide, o (3) se necesita salida de
audífonos balanceada (que el Aune ya cubre). Si aplica (1) o (2), **el SE ya lo hace** y
tiene precio conocido; no hace falta esperar al EX.

**Entregable:** `analisis_rme_dac_fs_vs_adi-2-4_pro_ex.md` en la raíz del proyecto, más un
diagrama SVG comparativo.

## ¿Cuál es el cuello de botella? (12:35)

Pregunta derivada del análisis anterior. Se levantó la cadena real en la máquina del
propietario con `wpctl`, `pw-metadata` y la lectura de su configuración de PipeWire.

**Corrección importante a la memoria:** `99-rme-fix.conf` **no fuerza 192 kHz**. Fija
`default.clock.rate = 48000` con `allowed-rates = [44100 48000 88200 96000 192000]` y
quantum 1024 (512/2048). Estado en vivo: 48 kHz / 1024. La nota vieja era obsoleta.
Corregido en MEMORY.md.

**Otros hallazgos verificados:**
- **Sink por defecto = «Audio Interno Estéreo digital (IEC958)», no el RME.** El RME está a
  volumen 1.00 (sin atenuación por software: correcto). **EasyEffects no corre.** `qemu` usa
  el RME como dispositivo de captura.
- `resampling.conf`: `resample.quality = 4` (valor por defecto de PipeWire, no el máximo).
- `alsa-hardware.conf`: headroom y period-size 2048, sin suspensión por inactividad.

**Potencia real del Aune S17 Pro EVO (manual local):** balanceada G-H → 7500 mW @32 Ω,
4654 @55 Ω, 2673 @100 Ω, 918 @300 Ω. No balanceada G-H → 2050 mW @32 Ω. Salida 1 Ω.
Los HE1000se (35 Ω, 96 dB) necesitan **~250 mW para 120 dB de SPL**: el Aune da ~6900 mW a
35 Ω, unos **14 dB de margen**. El amplificador no es cuello de botella.

**El PEQ está saturado.** El preset `HEKSE-HarmanV4` ocupa **las 5 bandas** (+2,5 @40 ·
+3,0 @1950 · −2,5 @3600 · −3,0 @6000 · −1,5 @8200) más shelf de graves +3,0 @105 Hz. Una
corrección Harman de esta clase necesita 7–10 filtros. Es el único límite real de la
electrónica, y se rompe gratis con EasyEffects (ya instalado, permite más bandas y
convolución). Además se comprobó que los `.adieqpr` **sólo guardan EQ**: el preamp de
−7,5 dB hay que ponerlo a mano.

**Veredicto entregado:** el cuello de botella no está en la electrónica. Orden: la
grabación y el máster → el formato de entrega → la audición del propietario → las
almohadillas y el sellado → el PEQ de 5 bandas → la cadena de software. Se listaron siete
acciones gratuitas ordenadas por rendimiento (confirmar el tier de la fuente, probar el
loudness a nivel real, la prueba del centímetro en las almohadillas, mover el PEQ a
software, poner el RME como sink por defecto, subir la calidad de remuestreo y alinear el
volumen entre RME y Aune).

**Entregable:** `cuello_de_botella_cadena.md`, más un diagrama SVG con el ranking.

## Fuente, remuestreo y pérdida de calidad (12:46)

Tres preguntas del usuario: cómo arreglar el remuestreo 44,1→48 y por qué importa; cuánto
pierde usando Cider con Apple Music y cómo saber el códec real; y si Spotify daría mejor
calidad.

**Corrección importante a lo que yo mismo había dicho antes.** Afirmé que el usuario tenía
«la calidad de remuestreo en el valor por defecto». **Es falso para la ruta que usa Cider:**
`pipewire-pulse.conf.d/force-192k.conf` fija `resample.quality = 10` para todos los clientes
PulseAudio (Cider, Chromium, Zen, Steam, qemu), y su comentario aclara que el nombre es
histórico y ya no fuerza la frecuencia. Sólo los **clientes nativos** siguen en 4
(`client.conf.d/resampling.conf`). Rango verificado en `pipewire-props(7)`: **0 a 14**,
por defecto 4.

**Y rebajé la importancia del tema.** Con calidad 10 el error de remuestreo queda por debajo
del piso de ruido del propio RME (123 dBA ≈ −123 dB). En el ranking anterior lo puse en el
puesto 6 como «menor pero real»; debí bajarlo más: **no es audible**. La razón para tocarlo
es que es el único paso de procesamiento evitable, y cuesta una línea.

**Cider:** versión 4.0.9.1 (paquete `cider`, Genten). Es Electron y en su directorio de datos
hay un `CdmStorage.db` (DRM), lo que encaja con que envuelva el **reproductor web de Apple
Music**. El reproductor web **no entrega sin pérdida**, y **Apple no tiene cliente para
Linux** (su lista oficial de plataformas con sin pérdida excluye Linux). Conclusión: Cider
entrega **AAC-LC ~256 kbps** siempre, se pague lo que se pague.

**Niveles de Apple Music (verificados):** HE-AAC ~64 · AAC-LC ~256 · ALAC hasta 24/48 ·
ALAC Hi-Res hasta 24/192 · Atmos E-AC-3+JOC ~768 kbps. Medición citada: un tema de 221 s
ocupa 7,9 MB en AAC-LC (≈286 kbps medidos) y 44,2 MB en ALAC 24/44,1 (≈1.600 kbps).

**Spotify:** lanzó sin pérdida el **10-sep-2025**, **FLAC hasta 24 bits/44,1 kHz**, incluido
en el **Premium normal**, disponible en **México**, y soportado por el **cliente de
escritorio desde la 1.2.67**. En sep-2025 el cliente de Linux se quedó en 1.2.63 y hubo queja
formal; **esa brecha ya se cerró** (1.2.74 en nov-2025 y un usuario de Debian confirmando sin
pérdida). Matices dados: es calidad de CD, no alta resolución; Spotify aplica normalización
de intensidad incluso en sin pérdida; y el sin pérdida no cubre vídeos, podcasts ni
audiolibros.

**La mejor vía, y ya la tiene: el DMP-A6.** Tidal/Qobuz desde el Eversolo, salida digital al
RME: sale el PC, sale PipeWire, sale el remuestreador. Limitación: **no tiene Spotify
Connect**, así que si elige Spotify la ruta es el cliente de escritorio del PC.

**Métodos para verificar el códec real entregados:** DevTools dentro de Cider (Network,
buscar `.m3u8`) y, sobre todo, el **caudal de red** (AAC 256 ≈ 32 KB/s; sin pérdida 16/44,1
≈ 176 KB/s), que es decisivo y no se puede falsear.

**Entregable:** `fuente_remuestreo_y_perdida.md`, más un diagrama SVG comparando las tres
vías de fuente.

## Remuestreo a 10 y verificación del grafo real (13:06)

El usuario pidió **ejecutar** el cambio (no describirlo): subir `resample.quality` de 4 a 10
en `~/.config/pipewire/client.conf.d/resampling.conf` y reiniciar PipeWire, y después
verificar qué hace el grafo de verdad con Spotify sonando.

**Ejecutado y verificado:**
- Archivo editado (4 → 10) con comentario explicativo. `systemctl --user restart pipewire
  pipewire-pulse wireplumber` → los tres servicios `active`, sin errores en el journal.
- `pw-metadata`: `clock.rate = 48000`, quantum 1024, `force-rate = 0`.
- `pw-dump`: **`"resample.quality": 10` en 5 nodos**. Las dos rutas quedan por fin iguales.
- **El grafo sí remuestrea:** Spotify entrega 44 100 Hz (`pw-top`: `F32LE 2 44100 = spotify`;
  `pactl list sink-inputs`: `float32le 2ch 44100Hz`) y el sink que alimenta corre a
  48 000 Hz (`S32LE 2 48000`). Confirmado el 44,1 → 48.

**Hallazgo importante — el RME está fuera del circuito.** El sink por defecto es
`alsa_output.pci-0000_00_1f.3.iec958-stereo` («Audio Interno Estéreo digital (IEC958)» de la
placa madre, `RUNNING` a 100 %, sin mute). `pw-link -l` confirma
`spotify:output_FL -> …iec958-stereo:playback_FL` (igual en FR). El sink del RME
(`…usb-RME_ADI-2_DAC…pro-output-0`) está **SUSPENDED y sin ningún enlace**. EasyEffects no
corre. → Mientras se escucha Spotify, toda la cadena RME → Aune → HE1000se está **inactiva**.

**Hallazgo importante — la calidad de Spotify está en 160 kbps, no en sin pérdida.**
Se leyó `~/.config/spotify/Users/1280065891-user/prefs`:
`audio.play_bitrate_enumeration=3` y `audio.play_bitrate_non_metered_enumeration=3`,
`audio.allow_downgrade=false`, `audio.normalize_v2=false` (normalización apagada).
La escala se **verificó en el propio binario** (enumeración protobuf `spotify.audiophile`):
`UNKNOWN=0 · LOW=1 · NORMAL=2 · HIGH=3 · VERY_HIGH=4 · LOSSLESS=5`. El hilo de la comunidad
de Spotify confirma con registro de audio que **VERY_HIGH = 320 kbps Ogg Vorbis** y
**HIGH = 160 kbps**. El binario contiene además `FLAC_FLAC`, `MP4_FLAC` y `BUCKET_LOSSLESS`.
→ El usuario está en **HIGH = 160 kbps**. Versión del cliente **1.2.96.518.g366879e1**
(muy por encima del mínimo 1.2.67), así que el sin pérdida está disponible pero **no activado**.

**Medición que descarta el sin pérdida:** caudal sostenido de la interfaz = **11 MB en 180 s**
(≈63 KB/s brutos, ~35 KB/s de fondo en pausa). El sin pérdida necesitaría ≥15 MB en esa
ventana. La caché (`~/.cache/spotify/Data`) está **cifrada**, así que los bytes mágicos no
sirven para identificar el códec; y no creció durante la reproducción, por lo que tampoco
sirve medirla. `playerctl` permitió confirmar reproducción real («Walkin' On The Sun»,
Smash Mouth).

**Entregable:** este registro + un diagrama SVG del grafo real y de la escala de calidad.

## Enrutamiento: todo el audio al RME (13:30-13:50)

El propietario pidió que **todo** el audio pase por el RME y la lista de excepciones.

**Causa encontrada.** El sink por defecto estaba **fijado a mano** a la salida digital de la
placa madre (`default.configured.audio.sink`), y WirePlumber da **+30000 de prioridad** al
nodo así marcado (`find-selected-default-node.lua`). Por eso ganaba la placa madre
(736+30000=30736) al RME (1600). Además, de 24 entradas con destino guardado en
`stream-properties`, **23 apuntaban a nodos inexistentes** (20 a `easyeffects_sink`, más
`easyeffects_source`, `…RME…analog-stereo` y `…hdmi-stereo-extra1`) y caían al defecto.
**Cider** era la única con destino válido y apuntaba a la placa madre.

**Corregido:** `pactl set-default-sink` → RME (persiste en `default-nodes`); eliminados los 23
destinos obsoletos conservando 121 entradas de volumen; movidas las reproducciones activas.
Copia de seguridad en `~/.local/state/wireplumber/backup-2026-09-20/`. Verificado con
`paplay` (PulseAudio) y `pw-play` (nativo): ambos al RME; durante la reproducción el RME está
`RUNNING` y todo lo demás `SUSPENDED`.

**Excepciones que quedan** (lista completa en el entregable): 4 salidas HDMI/DP de la GPU;
la salida digital de la placa madre; el mando DualSense Edge, un DAC USB Burr-Brown y el
DMP-A6 como tarjeta de sonido del PC; `auto_null`; la **entrada** del RME como fuente por
defecto; y la trampa de **EasyEffects** — instalado pero sin correr; si algún día se arranca
creará `easyeffects_sink` y puede llevarse 20 aplicaciones. No hay bypass por ALSA directo
(no existen `~/.asoundrc` ni `/etc/asound.conf`; mpv sin `ao=`; ningún proceso con
`PULSE_SINK`).

**Bit-perfect: no lo es.** Medido con el RME como destino: material de 44,1 kHz → el RME
corre a **48 000 Hz**; material de 48 kHz → 48 000 Hz. El RME está **fijo en 48 kHz**.
Probado y **descartado** que la causa fueran `session.suspend-on-idle`/`node.pause-on-idle`
(se comentaron, se reinició, siguió a 48 kHz; config restaurada). `clock.rate` en caliente:
no concluyente. Sí se verificó que `clock.force-rate 44100` hace que el RME corra a
`S32LE 2 44100` → **el hardware sí sigue la fuente, el grafo no lo hace solo**. La causa
última quedó sin identificar.

**Cider medido, ya no inferido.** Se parseó el descriptor de códec (`esds`) de su caché
(origen `aod-ssl.itunes.apple.com`, MP4 marca `iso5` con `hlsf`): **AAC-LC, avgBitrate
256 000 bps, 44,1 kHz, estéreo**. Confirma la deducción anterior. Cider entrega su audio a
PipeWire a **48 000 Hz** (Chromium remuestrea dentro de la app), mientras Spotify entrega
44 100 Hz nativo. Comparación: Cider 256 kbps AAC > Spotify 160 kbps Vorbis en la
configuración actual; Spotify pasaría por delante en Very High (320) o Lossless.

**Sin pérdida sin pagar:** no existe programa que lo consiga. El bucket lo elige el servidor
según el derecho de la cuenta; los 160 kbps son exactamente el **techo del plan gratuito**.
Alternativas reales: archivos propios por el DMP-A6 (bit-perfect, gratis).

**Entregable:** `enrutamiento_audio_al_rme.md`.

## Incidente: se quedó sin audio (14:05-14:10)

**Síntoma:** el propietario avisó «me dejaste sin audio» a las 14:06.

**Causa: EasyEffects.** Arrancó a las **14:05:19** y su `outputDevice` estaba configurado a
`alsa_output.pci-0000_00_1f.3.pro-output-1`, **un nodo que ya no existe** (la tarjeta de la
placa madre pasó de `pro-audio` a `iec958-stereo` y el nombre quedó huérfano). Como tiene
`useDefaultOutputDevice=false`, no cayó al dispositivo por defecto. Resultado: se llevó Cider,
Zen y mpv a `easyeffects_sink` y **no entregaba el audio a ninguna parte**. El RME quedó en
`IDLE` y la placa madre en `SUSPENDED`.

**No lo causó el cambio de enrutamiento:** el RME estaba `RUNNING` con Cider reproduciendo
justo antes. Era la trampa nº 20 que el informe ya había señalado — y se activó.

**Arreglo:** `outputDevice` apuntado al RME (`…usb-RME_ADI-2_DAC…pro-output-0`), copia en
`easyeffectsrc.bak-2026-09-20`, EasyEffects reiniciado. Cadena verificada con `pw-link -l`:
apps → `easyeffects_sink` → `ee_soe_output_level` → RME `playback_AUX0/AUX1`. Todo `RUNNING`,
sin mute, al 100 %. (Al cerrar EasyEffects los reproductores cayeron solos al RME y el audio
volvió al instante: eso confirmó el diagnóstico.)

**Pendiente y avisado:** `[EffectsPipelines] bypass=true` → EasyEffects **no está procesando
nada**, el PEQ no actúa. Desactivar el bypass cambia el sonido, así que es decisión suya. Y el
fallo puede repetirse si cambia el perfil del RME; `useDefaultOutputDevice=true` sería inmune
pero no se probó.

**Regla nueva para el proyecto:** si el audio desaparece y el sink por defecto es correcto,
**comprobar primero EasyEffects** (¿corriendo? ¿a qué dispositivo apunta su salida?).

## Mantenimiento de memoria

`MEMORY.md` volvía a superar el límite y se truncaba en la inyección (9.4 KB frente a un
corte real de 9.632 bytes). Primer intento de reescritura: quedó en 14 KB y luego en 9.8 KB,
es decir **peor**. Solución aplicada: **dividir**.

- `MEMORY.md` → **5.367 bytes**. Sólo convención, PC, PipeWire, fuentes de música, cuello de
  botella, estado de la serie RME EX y un índice de dónde está cada cosa.
- `referencia-equipos.md` → **5.836 bytes** (nuevo). Fichas estáticas: HE1000se, Aune,
  RME DAC FS, ADI-2/4 Pro EX, Topping, DMP-A8 Gen 2, inalámbricos y equipos sin BT de salida.

Regla para el futuro: **no volver a meter fichas de equipos en `MEMORY.md`**; van a
`referencia-equipos.md` y en `MEMORY.md` queda sólo el puntero. Sección de PipeWire reescrita
para reflejar que las dos rutas de remuestreo están en 10, y para registrar el enrutamiento
real del sink y la escala de calidad de Spotify.

## Bug de descargas offline de TIDAL (13:33)

El usuario tenía TIDAL Premium y dejó la app de iOS porque se colgaba descargando una canción
y no seguía con las demás. Reprodujo el mismo error en un Galaxy S21 Ultra arrumbado. Pregunta:
¿sigue el bug, es por las listas gigantes, hay solución?

**Respuesta: el bug sigue vivo y el tamaño de la biblioteca no es la causa.**

- Versión iOS actual **2.215.0** (16-sep-2026), notas de versión genéricas, **sin mención a
  descargas**. La ayuda oficial de modo offline en iOS lleva **4 años sin actualizar** y no
  documenta el problema. TIDAL no lo reconoce ni anuncia arreglo.
- Reseñas reales (App Store MX/US/GB, v2.199–2.215) con el mismo síntoma. Las más útiles:
  v2.205.0 MX «directamente las bloquea y de ahí no pasa la descarga»; v2.214.1 GB «album
  downloads stop working… a known issue for a very long time»; v2.200.0 GB «it's just been
  bumped to the back of the queue». **Caveat registrado: sesgo de muestra, mide persistencia,
  no prevalencia.**
- **Mecanismo:** la cola no aísla el fallo (sin timeout por elemento ni reintento individual) →
  un elemento que falla bloquea a todos los siguientes. Y hay elementos que fallan del lado de
  TIDAL: el changelog de `tidekeeper` (cliente independiente) documenta estados **«asset not
  ready»**, **HTTP 404 permanente**, **HTTP 429** y **`CLIENT_NOT_ENTITLED`** — llegó a
  reintentar hasta 64 veces antes de acotarlo a 6. En una biblioteca grande la probabilidad de
  topar con uno tiende a 1.
- **Segundo defecto:** la descarga corre en el proceso de la app, no en una sesión en segundo
  plano. Apple documenta que `URLSession` en segundo plano **sí** sigue con la app suspendida;
  todas las reseñas dicen lo contrario de TIDAL («the app needs to be open»). Que TIDAL no lo
  use es **inferencia**, no confirmación.
- **Por eso falla igual en el S21 Ultra:** el fallo es del cliente, no de iOS; One UI solo lo
  agrava con la optimización de batería.
- **Dato de los 0,5 TB:** el mismo tamaño son ~3.300 pistas en Max (FLAC 24/192) o ~52.000 en
  High (AAC 320). En Max no es una biblioteca gigante → el tamaño queda descartado como causa.
  Estimación propia, supuestos declarados en el entregable.
- **Descartado como evidencia:** un blog automatizado que afirmaba que TIDAL arregló las
  descargas en segundo plano en junio de 2026 (enlace 404, sin respaldo en notas de versión).
- **Ajuste de calidad:** existe un ajuste separado `Settings > Downloads > Audio` (Low/High/Max)
  y **sólo aplica a descargas futuras** — para cambiarlo hay que borrar y volver a bajar.
- **Recomendación dada:** partir el trabajo — Apple Music para offline (gestiona la descarga a
  nivel de sistema) y TIDAL para la cadena de escritorio. Qobuz mencionado pero **no verificado**.

**Entregable:** `tidal_descargas_offline_bug.md` + diagrama SVG del mecanismo.

## Comparativa lossless en Arch + 15.000 offline (13:48)

Petición: añadir Qobuz y dos servicios más que permitan (a) lossless en Arch Linux y (b) mantener
~15.000 canciones descargadas en el teléfono; más datos, curiosidades, mejores algoritmos y en
quién confían los audiófilos.

**Titular: ningún servicio cumple las tres condiciones.** Cada uno falla en un sitio distinto.

- **Spotify tiene un tope duro de 10.000 canciones descargadas por dispositivo** (5 dispositivos) y
  exige reconectar **cada 30 días**. Fuente: ayuda oficial en español. → **15.000 no caben.** Además
  el plan gratuito sólo puede descargar pódcasts, y no se pueden descargar canciones sueltas.
- **Qobuz no tiene tope de pistas** (sólo limitado por almacenamiento) y **las descargas no se borran
  al cerrar sesión ni al reinstalar** (ayuda oficial, act. 5-dic-2025). Es lo contrario de Spotify y
  de TIDAL. Precio México: **$149/mes o $1.498,80/año = $124,90/mes**. Paga **$0,018732 por
  reproducción** (4,4× la media del mercado).
- **Golpe del día: QBZ fue retirado hoy.** El cliente nativo de Qobuz para Linux (Rust, bit-perfect,
  gapless, DSD por DoP), obra de un desarrollador **mexicano**, ya no existe: GitHub 404, AUR
  `qbz-bin` no existe (verificado por API RPC v5), Snap 404, Flathub retirado. `qbz.lol` devuelve
  200 con un aviso: «QBZ Paused · En pausa… el repositorio, las versiones y los paquetes en Flathub,
  Snap, AUR y mis repos de apt y rpm fueron retirados. No sé todavía si el desarrollo continúa».
  → Para Qobuz en Arch quedan el **DMP-A6** (la vía bit-perfect real), Roon Server y LMS.
- **Aritmética que reencuadra el problema:** 15.000 pistas son ~144 GB en AAC 320, **~405 GB en FLAC
  16/44,1**, ~1,1 TB en 24/96 y ~2,25 TB en 24/192. **No cabe en un teléfono.** El requisito realista
  es «15.000 en el teléfono, sin pérdida en casa». (Coincide con su «casi medio tera» de Apple Music.)
- **Descartados por motivos duros:** Apple Music (no hay cliente Linux; Cider = AAC 256 medido),
  Amazon Music (sin cliente Linux y web limitado a SD), Deezer (sin app oficial Linux; México no
  lista plan HiFi — sólo Premium US$11,99 y Family US$19,99).
- **Algoritmos — estudio ACM CHIIR 2022 «The Diversity of Music Recommender Systems»**, con método
  objetivo (listas de 20 canciones en 3 niveles de diversidad, primeras 30 sugerencias, factorización
  matricial + similitud coseno; menos similitud = más diversidad): **YouTube Music el más diverso
  (0,259)**, Apple 0,287, Last.fm 0,310, Pandora 0,361, **Spotify el menos diverso (0,479)** y el
  único que **reduce** la diversidad de la entrada. Matiz: diversidad ≠ calidad; Spotify gana en
  enganche. Y de 1.000 reseñas analizadas sólo 27 hablaban de recomendaciones: la gente no distingue.
- **Curiosidades registradas:** Spotify y Apple Music cuestan **exactamente $139** el plan individual
  en México; Qobuz anual sale $14/mes **más barato** que Spotify Individual; Deezer Free no está
  disponible en México; Apple subió precios el 18-jul-2026 sin avisar; TIDAL abandonó MQA y 360 RA
  el 24-jul-2024 tras la administración de MQA Ltd; Qobuz le dijo «coming soon» a un cliente Linux
  en 2023 y en 2026 sigue sin existir.
- **Recomendación dada:** Opción A (Qobuz solo, $124,90/mes, offline en el teléfono + DMP-A6 en el
  escritorio); Opción B (Apple Music + Spotify Premium, $278/mes, máxima comodidad en Linux pero
  calidad CD y tope de 10.000); Opción C descartada por pagar TIDAL con el offline roto.
- **Sin verificar:** si Qobuz permite comprar música en México (el desarrollador de QBZ dice que no;
  la web mexicana anuncia «tienda de descargas Hi-Res» — **las fuentes discrepan**), el estado real
  de Deezer HiFi en México, los límites offline de Deezer y Amazon, y el motivo de la retirada de QBZ.

**Entregable:** `comparativa_lossless_offline_15k.md` + diagrama SVG con la matriz de criterios.

---

## Mapa de salidas del sistema y «por qué Zen no suena» (14:30–14:45)

**Pregunta del usuario:** «cerré easyeffects para que no procese mi audio, mandé audio a distintos
[dispositivos], solo se oye audio interno estéreo digital de Cider, Zen no se oye nada, además tengo
muchas salidas NAVI pero solo la pro 7 que es mi bocina del monitor sirve, las demás son necesarias o
por qué están ahí? No entiendo las salidas que tengo explícalas».

- **Las seis salidas que existen:** cuatro de la GPU (`pro-output-3/7/8/9`), la digital de la placa
  madre (`iec958-stereo`) y el RME (`…RME_ADI-2_DAC…pro-output-0`).
- **Por qué hay cuatro «Navi»:** no son cuatro tarjetas, es **una GPU con cuatro conectores**. La
  tarjeta está en perfil **`pro-audio`** (`pactl list cards`), que expone cada salida en crudo en vez
  de un único `hdmi-stereo`. Es consecuencia del perfil, no un fallo.
- **Cuál funciona lo dice el ELD, no una suposición.** `/proc/asound/card2/eld#0.1`:
  `monitor_present=1`, `monitor_name = LG ULTRAGEAR+`, `connection_type = DisplayPort`. Los otros tres
  (`eld#0.0`, `0.2`, `0.3`) dan `monitor_present=0`. **Mapeo AMD: `eld#0.N` ↔ `pro-output-<3+4N>`.**
  → **pro-7 es la única con cable, y coincide exactamente con lo que el usuario observó.** Las otras
  tres no son necesarias y no se pueden borrar sin cambiar el perfil de la tarjeta.
- **El monitor declara** por ELD: LPCM hasta 8 canales a 32/44,1/48/96/192 kHz, 16/20/24 bits
  (declarado, no medido).
- **Zen estaba mudo por dos causas independientes:** (1) sus tres flujos (`#797`, `#1536`, `#2072`)
  estaban en el sink `1257` = **la S/PDIF de la placa madre**, no el RME, porque WirePlumber había
  vuelto a guardar `Zen -> …iec958-stereo`; (2) los flujos estaban **`Corked: yes`** (pausados por la
  propia app). Un reproductor en pausa no suena aunque el enrutamiento sea perfecto.
- **Los destinos fantasma volvieron.** Tras la limpieza de las 13:30 había **0**; hoy había **4**:
  `Zen`, `Cider` y `PipeWire ALSA ompv` → placa madre, y `mpv` → `easyeffects_sink` (**nodo
  inexistente**). **Lección: mover una app a un dispositivo en el panel no es temporal — queda grabado
  y esa app vuelve ahí siempre, aunque el dispositivo no tenga nada conectado.** Eliminados los cuatro
  (143 entradas de volumen conservadas); copia en `backup-2026-09-20/stream-properties.1433`. Al
  reiniciar WirePlumber los tres flujos de Zen cayeron solos al RME.
- **Estado final verificado:** sink por defecto = RME; los tres flujos de Zen en el RME (`sink 1802`);
  la S/PDIF de la placa madre `SUSPENDED`; **0 destinos guardados**.
- **CORRECCIÓN de una lectura mía anterior (importante):** en el incidente de las 14:05 interpreté que
  `/proc/asound/card0/pcm0p/sub0/status` en `RUNNING` con `hw_ptr` avanzando **probaba** que el audio
  llegaba al RME. **Estaba mal.** El propio `alsa-hardware.conf` del usuario fija para el RME
  `session.suspend-on-idle=false` y `node.pause-on-idle=false`, así que **PipeWire nunca pausa el
  dispositivo**: el PCM queda `RUNNING` aunque no le llegue ni una muestra. `RUNNING` en `/proc/asound`
  **no distingue «suena» de «abierto en silencio»**. El `owner_pid` (231125) es `pipewire`, que abre
  también el PCM de la placa madre. Para saber si algo suena: estado del nodo en PipeWire + `Corked`.
- **Sin resolver:** el usuario dice que oía Cider cuando estaba en «Audio Interno Estéreo digital
  (IEC958)». Eso no cuadra con que las otras tres Navi estén sin monitor ni con que el RME sea el que
  tiene el Aune y los HE1000se. **Falta que el usuario diga qué hay enchufado a cada salida** — es la
  única pieza del mapa que no se puede leer desde software.

**Entregable:** `enrutamiento_audio_al_rme.md` §8 (nuevo) + §9 ampliado + diagrama SVG del mapa de
salidas. Skill `pipewire-grafo-y-codec-streaming` ampliada con §3-quinquies (método ELD, mapeo
`eld#0.N ↔ pro-output-<3+4N>`, y la trampa de `RUNNING` en `/proc/asound`) y secciones reordenadas.
MEMORY.md depurado de 9.075 a 8.068 bytes para dejar de truncarse.

---

## Dos flujos de Zen, y el extraño «Sync Source: SPDIF» del RME (14:46–15:00)

**Pregunta:** «tengo dos videos y solamente el que sale reproducido por iec958 se escucha en mis
audífonos, esto es como debe ser?». Captura: dos flujos «Zen: AudioStream» — uno en **ADI-2 DAC
(51100523) Pro** y otro en **Audio Interno Estéreo digital (IEC958)**.

**Lo medido (no interpretado):**
- Los flujos eran `#797` y `#1536` en el RME (sink 1802) y `#2297` en la placa madre (sink 1804).
- **Los tres con `Corked: yes`** → estaban en pausa. Al medir después, 2 corked y 1 sonando.
  **Un reproductor en pausa no suena aunque el enrutamiento sea perfecto.**
- El media.name del RME era `(124) Deepseek just did the impossible - YouTube`; los otros dos
  `AudioStream` genérico. Son **pestañas distintas de Zen**, no un fallo.
- **El nodo del RME estaba `running`** (no suspended) y `pw-link` mostraba `Zen:output_FL/FR →
  RME:playback_AUX0/AUX1`. **El flujo del RME sí tenía camino eléctrico real.**
- **`amixer -c 0 sget 'Sync Source'` = `SPDIF`**, con `SPDIF Sync = Sync` y `SPDIF Rate = 48000 Hz`,
  mientras el USB también corría a 48000. **Dato raro que hay que explicar, no ignorar.**

**Interpretación honesta:** el ADI-2 DAC FS tiene **UNA sola entrada digital óptica/coaxial**. Que
`Sync Source` diga SPDIF y `SPDIF Sync` diga `Sync` **sugiere que hay un cable en esa entrada y algo
conectado emitiendo a 48 kHz** (candidato obvio: la salida S/PDIF de la placa madre, que es la misma
que desviaba el audio antes). El RME, además, guarda `Sync Source` en su **propia memoria** — no lo
gestiona PipeWire, así que puede ser un ajuste heredado. **No se pudo determinar desde software si el
reloj efectivo lo manda el SPDIF o el USB.** Lo que sí es seguro: USB y S/PDIF corren ambos a 48 kHz,
así que **la frecuencia no distingue**, pero el **jitter sí** — un reloj S/PDIF de placa madre es muy
inferior al reloj del USB, que es síncrono y manda el RME.
**Acción pendiente del usuario (no se puede hacer por software):** poner `Sync Source = Internal` en el
menú del RME, o desconectar el cable óptico, y volver a medir. Es un cambio de sonido suyo.

**Respuesta a «¿es como debe ser?»:** **No.** Lo correcto es **un solo flujo por pestaña**, todos al
RME, y **la pestaña que no se está viendo no debería estar emitiendo**. Directorio: `Zen: AudioStream`
en la placa madre está mal; solo el del RME es el bueno.

**Entregable:** `enrutamiento_audio_al_rme.md` §8 ampliado con el caso y el hallazgo del Sync Source.

---

## CORRECCIÓN IMPORTANTE: la cadena real es placa madre → cable óptico → RME (15:00)

**El usuario corrige:** «rme está recibiendo por cable óptico la señal de audio, también lo tengo
conectado por usb al pc para que reciba las instrucciones de cambio de volumen y poder modificar
perfiles con adi-2-remote, cuando selecciono la iec958 sí tengo sonido con la otra no».

**Esto invalida mi interpretación anterior.** Yo trataba la S/PDIF de la placa madre como el
enemigo a eliminar y el RME-por-USB como el destino correcto. **Es al revés: es una cadena
deliberada y funcional.**

**La cadena real, verificada:**
```
apps (Zen, Cider) → sink 1804 = placa madre iec958-stereo (S/PDIF) → CABLE ÓPTICO → RME
                                                    ↕
                                        USB = SOLO CONTROL (volumen, perfiles, ADI-2 Remote)
```
- **Sink 1804 (placa madre S/PDIF) = `RUNNING`** — es el que transporta el audio de verdad.
- **Sink 1802 (RME por USB) = `IDLE`**, con **0 enlaces** `playback_AUX`. El USB no lleva audio.
- **`Sync Source = SPDIF` + `SPDIF Sync = Sync` + `AES Sync = No Lock`** → el RME está enganchado
  al reloj que le llega por el cable óptico. **Es coherente y correcto para esta cadena.**
- El PCM del RME por USB está `RUNNING` pero por el control (volumen/perfiles), no por audio —
  encaja con `node.pause-on-idle=false`.

**Mi error de razonamiento:** vi `SPDIF Sync = Sync` y lo interpreté como «hay una señal parásita
que hay que quitar». **Era la señal que el usuario quiere.** También asumí que el USB debía llevar
el audio porque es el camino «técnicamente superior», sin comprobar que su montaje es otro.
**Regla aprendida: antes de llamar «problema» a un enlace, preguntar qué cable hay y para qué.
Un ajuste raro puede ser una decisión deliberada.**

**Matiz técnico — CORREGIDO a las 15:45.** Lo que escribí aquí («el jitter es el de la placa madre»)
**está mal enfocado y lo retiro.** El manual del ADI-2 v1.8 §31.3 dice que la conversión D/A «always
operates on highest sonic level, **being completely independent from the quality of the incoming
clock signal**». SteadyClock FS regenera el reloj. Además §14.1.2 aclara que la fuente de reloj «is
automatically determined and set by the unit, **a selection is neither possible nor necessary**»:
el `Sync Source = SPDIF` de la pantalla es una **lectura, no un ajuste**, así que el usuario no lo
cambió — no puede. Detalle completo en `enrutamiento_audio_al_rme.md` §8.9.

**Entregable:** `enrutamiento_audio_al_rme.md` §8.8 (cadena real) y §8.9 (reloj y jitter).

---

## Sesión 15:19–15:50 — revisión del jitter y barrido de correcciones

**Lo que pidió el usuario:** revisar «muy, muy profundamente» mi frase «el jitter es el suyo […]
es un factor menor», y **corregir todos los documentos del proyecto** donde hubiera información
errónea. Aportó el dato decisivo: con **USB** oía **distorsión «robótica» de ~1 s varias veces por
hora**; con el **cable óptico**, «un par de veces al día, si acaso» — y ese día ninguna.

**Lo que hizo el asistente:**
1. `pdftotext` del manual del RME → leídas §§12.1, 14.1.2 y 31.3. Las tres desmienten mi postura.
2. Escrito **§8.9** en `enrutamiento_audio_al_rme.md` con cinco subapartados.
3. **Barrido de correcciones en 9 documentos** (ver abajo).
4. **MEMORY.md** reescrito y recortado de 9.394 → **8.166 bytes** (dentro del límite de 8.192).
5. Skill `pipewire-grafo-y-codec-streaming` ampliada con **§3-0** (preguntar qué cable hay antes de
   diagnosticar un enlace) y con la regla de marcar apartados superados en vez de borrarlos.

**Barrido — archivos tocados:**

| Archivo | Cambio |
|---|---|
| `enrutamiento_audio_al_rme.md` | **§8.9 nueva** (reloj/jitter/`Sync Source`); avisos en §0, §8.1 y §8.7; banner general |
| `verificacion_grafo_pipewire.md` | Corregida: sus comandos recomendados **habrían silenciado el audio** |
| `cuello_de_botella_cadena.md` | Corregido el sink por defecto + **§1.1-bis nueva** (transporte óptico vs USB) |
| `audio_context.md` | **§6 corregido**: aclarado PC–óptico vs Eversolo–USB, tabla de ajustes recomendados, §4.8 del Eversolo |
| `old/audio_reference_2026-09-10.md` | Bloque de corrección sobre SteadyClock FS |
| `old/sistema_audio-2026-09-12.md` | Banner **[SUPERADO]** |
| `old/audio_rules (gemini 2026-02).md` | Banner **[SUPERADO]** |
| `old/audio_rules (arya stealth 2025).md` | Corregido el «rechazo >99.9 %»: **no es dato del fabricante** |
| `.workbuddy-ai/memory/MEMORY.md` | Reescrito y recortado |

**Revisados y SIN cambios necesarios** (no contienen el error de cadena ni afirmaciones de jitter):
`analisis_rme_dac_fs_vs_adi-2-4_pro_ex.md`, `analisis_rme_vs_eversolo_a8_gen2.md`,
`comparativa_lossless_offline_15k.md`, `tidal_descargas_offline_bug.md`, `old/audio_addendum.md`,
`fuente_remuestreo_y_perdida.md`, `cable_y_almohadillas_he1000se.md`, `top100-albums.md`.

**Conclusión de la revisión del jitter:** mi frase era errónea por dos motivos — (1) afirma una
dependencia del reloj de entrada que el manual niega explícitamente; (2) trataba `Sync Source` como
algo configurable cuando no lo es. **El propietario no está confundido; yo estaba equivocado.**
`Source = Auto` es el ajuste de fábrica y es el correcto para su objetivo (que el Eversolo entre
solo): §12.1, «In Auto Mode any detected SPDIF Signal will have priority over USB playback».

**Lo que sigue sin saberse — y no se va a inventar:** la **causa** de la distorsión «robótica» del
USB. Cuatro candidatos, ninguno verificado: (a) pérdida de paquetes/carga del bus USB, (b) gestión
de energía del puerto, (c) comportamiento del receptor ante interrupción, (d) pérdida momentánea del
enganche de reloj. **Nunca se capturó un registro de errores durante un evento.** Lo que sí es firme
es **el hecho medido en escucha**: en esta instalación el óptico falla mucho menos que el USB.

## ¿Qobuz web player hace lossless? (15:26)

**Respuesta:** sí, entrega FLAC (16/44,1 y, según catálogo/plan, Hi-Res 24-bit hasta 192 kHz) —
**pero no es bit-perfect y no permite offline**. Entregable: `qobuz_webplayer_lossless.md`.

- **Lo que Qobuz NO documenta:** ninguna página de su centro de ayuda fija la calidad máxima del web
  player con una cifra. **No se inventa.**
- **Corrección importante:** el techo de **24-bit/96 kHz** que yo había asociado al web player es en
  realidad de **Google Cast/Chromecast** (§10202: «by using Google Cast/Chromecast built-in ... 24-bit
  at 96 kHz in most cases and up to 24-bit at 192 kHz on some devices»). Son cosas distintas.
- **Prueba estructural (MDN, `BaseAudioContext.sampleRate`):** «the sample rate ... **used by all
  nodes in this audio context**. **This limitation means that sample-rate converters are not
  supported**» → **un `AudioContext` tiene UNA sola frecuencia para todo el grafo**; el navegador no
  puede conmutar el reloj del dispositivo por pista como sí hace una salida exclusiva. Coincide con
  lo ya medido aquí en Cider (entra 44,1, sale 48). **Verificación casera de 30 s:** en DevTools,
  `new AudioContext().sampleRate` → si da 48000 con la tarjeta en 44,1, queda demostrado.
  **Matiz:** cuál es esa frecuencia depende del hardware/sistema/navegador (es un valor negociado),
  no es una constante universal; en Android/Chromium se documentó que se queda en la nativa.
- **Qobuz no tiene app de escritorio para Linux** (su página de ecosistema solo ofrece Windows y
  macOS). El camino Hi-Res que Qobuz documenta para PC es **WASAPI exclusivo o ASIO** — ambos
  inexistentes en Linux. En Linux quedan **web player** o **Qobuz Connect**.
- **HALLAZGO ÚTIL:** **Eversolo está en la lista oficial de marcas con Qobuz Connect** (lista
  actualizada el 3-abr-2026). Qobuz Connect manda «music directly from Qobuz servers to your device,
  in Hi-Res up to 24-bit/192 kHz» → **esquiva el cuello del `AudioContext`**. O sea: el usuario
  probablemente ya tiene el mejor camino de Qobuz en casa (**app → Connect → DMP-A6 → óptico → RME**)
  y el web player le sirve solo de mando. *Comprobarlo en los ajustes del DMP-A6: la marca en la
  lista no garantiza que la unidad concreta lo tenga.*
- `qbzd` (fork receptor de QBZ para Linux, usa PipeWire/ALSA/Pulse) **no afirma bit-perfect**; su
  único apunte de calidad es mostrar «192 kHz» en la app en vez de «44,1» por defecto. No fiable.
- **Chromecast Audio** soporta «FLAC (up to 96kHz/24-bit)» (documentación de Google Cast) → por ahí
  también se recorta el Hi-Res a 96 kHz.
- **Higiene de memoria:** `MEMORY.md` había pasado del límite y salía truncado al inyectarse.
  **Reescrito y consolidado** (8.644 bytes).
